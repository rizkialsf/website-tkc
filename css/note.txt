h1 : font-size: clamp(40px, 3.5vw, 64px);
h2 : font-size: clamp(32px, 3vw, 48px);
h3 : font-size: clamp(24px, 2.5vw, 36px);
h4 : font-size: clamp(18px, 1.5vw, 28px);
h5 : font-size: clamp(16px, 1.2vw, 20px);
h6 : font-size: clamp(14px, 1vw, 16px);

h1 {
  font-size: clamp(30px, 4.5vw, 80px);
  color: var(--header-text-color);
}
h2 {
  font-size: clamp(24px, 3.5vw, 56px);
  color: var(--header-text-color);
}
h3 {
  font-size: clamp(20px, 2.5vw, 38px);
  color: var(--header-text-color);
}
h4 {
  font-size: clamp(16px, 1.8vw, 28px);
  color: var(--header-text-color);
}
h5 {
  font-size: clamp(14px, 1.3vw, 22px);
  color: var(--header-text-color);
}
h6 {
  font-size: clamp(12px, 1vw, 16px);
  color: var(--header-text-color);
}